---
id: "RPT-019"
uid: ""
title: "Week 37 of 2026: the standards refound, the design system splits, the corpus loses a third"
type: report
subtype: rollup
status: active
version: "1.5.0"
created: "2026-09-09T00:45:00Z"
updated: "2026-09-09T20:30:00+02:00"
author: "ursa"
owner: "oracle"
guild: "Alchemists"
territory: "Archive"
tags: [rollup, week, standards, refoundation]
license: "CC-BY-4.0"
visibility: "public"
period: "2026-W37"
evidence_script: "scripts/telemetry.mjs"
evidence_head: "7c25653"
related: ["RPT-018", "STD-012", "PRO-017", "ADR-041"]
absorbs: ["ADR-004", "ADR-005", "ADR-023", "ADR-026", "ADR-027", "ADR-040", "MIS-146", "MIS-0146", "DBT-013", "DBT-015", "DBT-016", "DBT-022", "ADR-001", "ADR-035", "ADR-037", "ADR-038", "ADR-039", "PRO-010", "MIS-096", "MIS-0096", "MIS-112", "MIS-0112", "MIS-113", "MIS-0113", "MIS-124", "MIS-0124", "MIS-131", "MIS-0131", "MIS-134", "MIS-0134", "MIS-142", "MIS-0142", "MIS-123", "MIS-0123", "MIS-127", "MIS-0127", "MIS-153", "MIS-0153"]
---

# Week 37 of 2026

> **Summary:** The first weekly roll-up under `STD-012`. One mission closed
> this week and its record is deleted; its line is below. The week's own
> figures are the ones the mission set out to move.
> **Epistemic:** What survived the week is what changed a rule, opened or
> closed a debt, or produced an address — `DEF-003`.
> **Pragmatic:** A reader following `MIS-146` lands here.
> **Audience:** Agents · Oracles

---

## Closed this week

- **`MIS-146` — Normative refoundation** · `rule` · closed 2026-09-09.
  The standards library went from an MVP corpus that stated each rule in
  several places to an alpha corpus that states it once: `STD-009` is the
  single normative source (65 core rules, 23 with a guard), `STD-002` is
  superseded, every anti-deletion clause is gone and `ADR-041` makes git
  the archive. Eleven standards took the shape of `STD-TEMPLATE`; a guard
  (`T-11` in `check-templates.mjs`) keeps them there. The design system
  split into a canon (`CAN-008`, the direction) and a standard (`STD-008`,
  the values); the engineering standard split into a norm (`STD-005`) and
  its procedure (`PRO-016`). Three debts closed by construction and were
  deleted (`DBT-014`, `DBT-018`, `DBT-019`). Proof: #292, #294, #296,
  #298 and the PR that carries this report.

  Measured at `2677f01` → `7c25653` (this PR, before its telemetry commit) (`scripts/telemetry.mjs`,
  `tokens.by_dir` / `tokens.total`; words by `wc -w standards/STD-*.md`):

  | | start | end |
  |---|---|---|
  | `standards/` words | 40,908 (11 files) | 29,194 (12 files, `STD-012` added by #297) |
  | `standards/` tokens | 69,171 | 50,817 |
  | corpus tokens | 528,587 | 305,109 |
  | standards in template shape | 3/11 | 12/12 |
  | rules stated in two places | ≥6 | 0 |
  | internal changelog sections | 3 | 0 |

  What the mission assumed and was false: that the target (~18,000 words)
  was reachable by filing alone. Two thirds of the remaining excess is
  `STD-008` (10,330 words of value plus reasoning), and cutting it is
  design editing the Oracle has not asked for. Acceptance criterion 4 is met
  in its second form: the file records why the target was wrong, with
  numbers. Criterion 5 — words per binding rule — went from 599 to 449
  (`29,194 / 65`), not to 20–50: the rules are one-line in `STD-009` and the
  reasoning lives in the standards around them, which the criterion did not
  anticipate.

  Left open, each with its home: `STD-009` stays `draft` until the Oracle
  ratifies (`DBT-021`); `CAN-008` waits for the Oracle's signature; the
  `DBT-022` split of `STD-001` is still owed. The one-page programme that
  `ADR-043` and the rewritten `STD-007` open (#299) — rename the plates, cut
  every standard to a page, fifteen norms and six registers — was filed as a
  fourth phase of this mission the same night it closed. It is not carried
  by this line: it is a new programme and needs its own mission card.

## Closed this week — registers census (2026-09-09)

Two registers read against the tree at `14db208`. Every file below passed
`ADR-030`'s four tests; the body is in git.

- **`DBT-013` — Design System filename is dated but not frozen** · `debt` ·
  closed 2026-09-04, the file renamed to `STD-008-design-tokens.md` (#316).
- **`DBT-015` — GIT-045 guard condemns withdrawn** · `debt` · closed
  2026-09-05, `check-core-rules.mjs` made symmetric.
- **`DBT-016` — Section numbers are load-bearing** · `debt` · closed by
  construction: `check-section-citations.mjs` reports 0 unresolved, baseline
  empty, after the 499 citations moved to plates (#301) and `CIT-050`.
- **`DBT-022` — A glossary with three definitions** · `debt` · closed by
  #319, `STD-001` is the register.
- **`ADR-001` — The archive** · `rule` · superseded by `STD-006` since
  2026-09-06; no living document depended on it normatively.
- **`ADR-035` — A folder is a genre** · `rule` · the rule is `SER-004` in
  `STD-024`; `history/` never opened and the moves are in git.
- **`ADR-037` — Accessibility gate has one normative home** · `rule` ·
  `ARC-010` lives in the register `STD-015`; zero living citers.
- **`ADR-038` — The genre map is government** · `rule` · absorbed with
  `STD-002` into `STD-009` (`PRE-001..006`, #318); it cited the deleted file
  seven times.

- **`ADR-039` — Licensing obligations leave CAN-005** · `rule` · the rule is
  `STD-010`; zero citers. Unsealed and deleted the same afternoon.

- **`PRO-010` — How to Archive** · `protocol`, `draft` 0.10.0 since April ·
  every obligation it carried has a home: series and folders `STD-001`,
  identity `STD-018`, header `STD-016`, versioning `STD-019`, lifecycle
  `STD-020`, moving a document `SER-005/006` (`STD-024`), the four tests
  `ADR-030`. Nine living citers re-pointed; closed records untouched.

**Second census of decisions (2026-09-10, `5b1face`).** Six more decisions
whose every rule now lives as a plate:

- **`ADR-004` — Identifiers are opaque, sequential, permanent** · `rule` ·
  rules 1–4, 6 and 8 are `IDN-011..015` (`STD-018`) and `STD-001`; rules 5
  and 7 had no plate and become `IDN-016` (next number from the trunk) and
  `IDN-017` (qualify across repositories). One living citer, re-pointed.
- **`ADR-005` — The 13-series register** · `rule`, already a superseded
  amendment by its own title · the register is `scripts/lib/rules.json` and
  `STD-001`; zero living citers. Script comments keep the name as history.
- **`ADR-023` — Four terms, English** · `rule` · the vocabulary is
  `STD-001`'s glossary; the language had no plate and becomes `DOC-011`
  (`STD-007`). Three citers re-pointed, two tags removed.
- **`ADR-026` — Agents CC0, one debt register** · `rule` · `agents/*` had no
  line in `LIC-002` and gets one; the single register is `STD-001`'s row
  and `DEF-004`. Two citers re-pointed.
- **`ADR-027` — Header apparatus ratified** · `rule` · ratification is
  `ADR-043` since #318; declared absence is `STD-004` §5; "sign what
  machines enforce" is `GRD-006/007` (`PRO-013`). `ratified_by` on `STD-001`
  and `STD-004` now names `ADR-043`.
- **`ADR-040` — A done mission can be deleted** · `rule` · `MSN-004`
  (`STD-025`). One citer re-pointed.

`ADR-036` stays: seven living documents cite it by name as the record of the
renumbering (`former_id_note` in CAN-005/006/007, prose in CAN-003 and
CAN-005, the `STD-003` register, `SYS-003`, `AGENTS.md`). It states the canon
is seven; it is eight since `CAN-008`. `ADR-030` was amended (3.2.0): debt no
longer survives its close, per `DEF-004`.

**Nothing is sealed (2026-09-09, Oracle's instruction).** 23 documents moved
from `sealed` to `governed`: eight canon, twelve standards, two decisions, one
template. `AUT-006` now reads: canon changes by a decision record or an
approved pull request. `PRE-003` loses a rung.

## Closed this week — mission purge (2026-09-09)

Oracle-approved cut of the board, verified against the tree, not against
the cards' own claims. Seven cancelled, three closed `done`. Bodies in git.

- **`MIS-096` — NWOS gets versioned, organizations adopt** · `none` · 0/7,
  effort L, solves the sovereignty of adopters that do not exist yet.
- **`MIS-112` — Home page from the README** · `none` · aesthetic coherence,
  no operative urgency.
- **`MIS-113` — README index generated from sections** · `none` ·
  acceptance criteria empty since creation.
- **`MIS-124` — Close the territory vocabulary** · `none` · 76 `TBA` in
  August, 0 today, resolved by other missions' attrition. `MIS-101` takes
  `territory: Archive`; `lint-frontmatter` no longer names MIS-124 as owner.
- **`MIS-131` — Frontmatter history census** · `none` · defensive census
  with no follow-up action.
- **`MIS-134` — Closure guard** · `none` · never built; its own evidence
  showed the design failing against MIS-109.
- **`MIS-142` — Decisions to standards** · `none` · 2/10; the work happened
  under `ADR-043` instead.
- **`MIS-153` — Agent roster page** · `done` · `agent.astro` +
  `AgentView.astro` existed; build green, 7 agents in `dist/agent/`.
  `check:responsive` not run (no Chromium), recorded as a limitation.
- **`MIS-127` — Entropy reduction** · `done` · Oracle closes the open
  mandate. Ledger: 12 rows, −33,616 tokens / −21 files net.
- **`MIS-123` — Make ADR-028 real** · `done` · `HDR-032..036` verified live
  (0 findings). `visibility` vocabulary is in `STD-016`, not `STD-001` as the
  card said — noted, not silently corrected.

The three `done` cards stay on the board until the next roll-up deletes
them (`DEF-004`).

## Carried up

Nothing yet: this is the first weekly report. At the quarterly roll-up the
line above carries up as `rule`.
