---
id: "RPT-2026-08-24"
uid:
title: "Daily Report — 2026-08-24"
type: report
subtype: daily
status: published
version: "1.0.0"
created: "2026-08-24T09:00:00Z"
updated: "2026-08-25T01:00:00Z"
author: "ursa"
agent: "Ursa"
model: "claude-opus-5"
week: "W35"
tags: [report, daily, archive, glossary, restructure, signing, debt]
license: "CC-BY-4.0"
evidence_script: "scripts/resolve-citations.py"
evidence_head: "9b45016"
---
# Daily Report — 2026-08-24

> **Summary:** The archive acquired a vocabulary, a debt register with twenty
> entries, four measuring instruments and its first signature. Nothing was
> signed, and that was the ruling.
> **Epistemic:** What the session established, and what it only measured.
> **Pragmatic:** What the next session picks up, and what blocks each item.

---

## What closed

### The glossary exists — `S-001`, merged, unsigned

The archive had no description of its own filing system: 246 documents, 60+
frontmatter fields, nothing written about what any of them meant. `S-001` now
defines the series, the registration scheme, the controlled vocabularies and
the change thresholds.

It went through **four Oracle reviews** and came out at v2.4.0. It is merged and
**deliberately not signed** — see *What stays open*.

### Twelve `cancelled` missions became `frozen`

The five-state vocabulary left 12 missions outside it. Oracle ruling: `frozen` +
`freeze_reason: cancelled`, on the argument that **a withdrawn promise is still
evidence** — deleting them would erase that they were proposed and dropped.

Executed with `scripts/cancel_to_frozen.py`, idempotent, verified: `cancelled`
absent from the corpus, `frozen` reads 13. Filed as `D-016`, **RESOLVED with its
trace** — `debt/` is append-only, and a closed entry is worth more than one that
never existed.

### The README was rewritten — it had drifted four months

Eight findings, each verified before being written down: the repository was
named `numinia-digital-agents` in its own title; the link justifying the repo's
language was broken; **`web/`, the folder that serves numinia.org, was not
mentioned at all**; it taught `cancelled` and `backlog`, both withdrawn; and it
propagated `Functional Model`, retired from canon four months earlier.

**The entry point of the archive was a vector for `D-012`.** Registered as
`D-015`, whose closing condition is not "fix the README" — that is done — but
**a machine that catches the next drift**.

### The archive has its first signature

`pre-restructure-2026-08-24`, annotated, signed, on `9b45016`. Before it, `main`
carried zero signed objects — not through negligence but because no signing
identity was configured and no local clone existed.

### `ADR-004` was recovered

`S-001` cites it as the authority for *never renumber* — the rule protecting
1,617 plain-text references. **The ADR had been closed without merging**, so the
rule was being enforced from a document that was not in the repository.
Recovered verbatim from `160404c`.

### Four instruments, and each one was wrong first

| Script | Produces |
|---|---|
| `count-evidence.py` | Every figure `S-001` states |
| `phase0-inventory.py` | The restructure inventory, pinnable with `--at` |
| `resolve-citations.py` | Which cited identifiers resolve |
| `check-references.mjs` | Broken links (**not merged** — see below) |

**Every one of them reported wrong figures before it reported right ones**, and
in each case the instrument was at fault, not the corpus:

- `count-evidence.py` counted apparatus as record → `D-014`
- `phase0-inventory.py` counted agent *files* as folders (17 instead of 5), and
  could not reproduce its own published figures once it became a corpus document
- `resolve-citations.py` counted table rows, sub-missions and section headings
  as missing documents — 49 broken instead of 17
- A first attempt at authority-checking searched for **normative sentences** and
  returned **zero** on a corpus with seventeen breakages, because the most
  important citation in the glossary is `(ADR-004)` — in parentheses, no verb

That pattern is the session's main methodological finding, and the argument for
`evidence_script` / `evidence_head` being mandatory rather than a courtesy.

### The debt register went from zero to twenty entries

`D-001`…`D-020`. Two are notable for what they say about the system rather than
about a field:

**`D-018`** — nothing checked that a document cited as *authority* exists. That
is how a merged `S-001` came to rest on an unmerged `ADR-004`.

**`D-019`** — the Oracle's correction, and the sharpest of the day. The agent
reported being unable to verify the new signature and filed it as an environment
quirk. The Oracle:

> *"`allowedSignersFile` does not have to be my machine's configuration. It
> should live in the repository. **If only I can verify my signatures, the
> `sealed` threshold is not auditable** — and that is the half of `D-011`
> neither of us had seen."*

---

## What stays open, with its blocker

| Item | Blocked by |
|---|---|
| **`S-001` signature** | `MIS-109` — see below |
| **`MIS-109`** — canon | Deliberately not started this session |
| **Reference lint in CI** | `D-017` — agent has no `workflow` scope |
| **`security-policy`, `credential-map`** | Genre ruling → ADR |
| **`guilds/`** | ADR |
| **`operations/`'s four loose files** | Oracle undecided; three have zero inbound references |
| **Restructure phases 1–4** | All of the above |

### Why `S-001` is merged and not signed

The Oracle's ruling, and it is the most important decision of the day:

> *"Signing `S-001` today publishes a **signed** vocabulary declaring
> `S-NNN = standards` while `canon/INDEX.md` and `STANDARDS.md` declare
> `S-NNN = seminal`, with ten real documents and a relation graph behind them.
> A signed contradiction is the worst state to stop in — it is `D-012` again,
> and this time we would be creating it knowingly.*
>
> *A draft that contradicts canon deceives nobody. That is what a draft is."*

**The signature becomes an acceptance criterion of `MIS-109`.** `S-001` is
signed when canon stops contradicting it — and it will be the archive's first
genuinely `sealed` artifact, which is fitting for the document that defines what
`sealed` means.

### The citation backlog, measured

Merging the three open PRs takes the corpus from **17 unresolved identifiers /
88 citations** to **9 / 45**. Everything that remains is `S-002`…`S-010` — which
is exactly `MIS-109`'s scope, not a residue.

`D-018` **resolves on merge**; it is currently reported broken only because it
lives in a sibling branch.

---

## What the next session picks up

**`MIS-109` — make canon filable.** It absorbs four operations on the same
fourteen files, deliberately: read once, rule on genre once, verify once.

1. Nine seminal documents with no frontmatter at all
2. Seven filenames carrying spaces
3. Renumber `S-002`…`S-010` to `C-NNN` and rewrite the relation graph in
   `canon/INDEX.md` — 40 citations
4. `D-012`: the partial terminology edit and the 21 documents still carrying the
   retired term, four of them canon itself
5. And the finding surfaced today: `canon/INDEX.md` points at
   *Epistemic relations between….md*, renamed months ago without the index being
   updated

**Touching canon is the most irreversible operation in the plan**, which is why
it was not started at the end of a long session.

---

## Rulings recorded today

| # | Ruling |
|--:|---|
| 1 | `cancelled` → `frozen` + `freeze_reason`. Not debt, a decision |
| 2 | Nothing in the archive is immutable — replaced by four change thresholds |
| 3 | `S-` collision → option 1: `S-` belongs to `standards/`, `seminal_id` retires |
| 4 | Prefixes `S-`/`A-`/`O-`/`D-` stay; `STD-`/`AGT-`/`OPS-`/`DEBT-` rejected |
| 5 | `security-policy`, `credential-map`: wrong `type`, but that is a genre ruling — an ADR, not a move |
| 6 | Canon gets its own mission, before the restructure phases |
| 7 | Sign the restore-point tag **before** signing `S-001`, never after |
| 8 | `S-001` merged unsigned; the signature is an acceptance criterion of `MIS-109` |
| 9 | One branch at a time, and an explanatory HTML before every PR |

---

## Corrections the Oracle made to the agent

Recorded because they are the session's real content, and because an archive
that only records its successes is `D-012` waiting to happen.

- **The `type` map is not a judgement.** A refactor was proposed that would have
  moved three documents on the strength of their `type`. The first instinct had
  been the opposite error — rewriting the `type` to match the folder, which
  `S-001` §3 forbids. **Both were wrong, in opposite directions.** §3 now
  requires verifying the `type` against the document first.
- **`[PENDING]` was invented.** A third enforcement marker appeared in nine
  places, next to `[CI]` and `[MANUAL]`. It was comfortable: it said "decided,
  just not applied yet" and sounded like progress. `[MANUAL]` forces the
  admission that a rule is not enforcing itself.
- **The HTML claimed to be generated and was not.** It had already drifted:
  renumbered sections, and §9 dropped entirely. `render-glossary.py` exists
  because of that.
- **An auditability gap was filed as an environment quirk.** `D-019`.
- **Immutability was asserted and was false.** 14 of 14 canon documents carry
  more than one commit.

---

## Figures

Measured at `9b45016` with `scripts/count-evidence.py` and
`scripts/resolve-citations.py`.

| | |
|---|--:|
| Tracked documents (excl. `web/`) | 246 |
| Documents in the wrong folder | **2** |
| Identifiers cited | 1,860 |
| Unresolved before the open PRs | 17 · 88 citations |
| Unresolved after merging them | **9 · 45 citations** |
| Debt entries | 20 (1 resolved) |
| Rules with a machine guard | **1 of 11** |
| Signed commits on `main` | 0 · signed tags: 1 |
