{
  "name": "numinia-nwos",
  "private": true,
  "type": "module",
  "description": "The tree and its guards. The web app lives in web/ with its own package.json.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test \"scripts/test/*.test.mjs\" \"guards/test/*.test.mjs\" \"tools/test/*.test.mjs\"",
    "guards": "node scripts/run-guards.mjs"
  }
}
